# ポモドーロタイマーアプリケーション 実装機能一覧

1. タイマー機能
   - 25分作業タイマー／5分休憩タイマー
   - カウントダウン表示（円形プログレスバー）
   - タイマー開始・停止・リセット
   - タイマー状態管理（idle, running, paused, completed）

2. セッション管理
   - 作業・休憩セッションの記録（開始・終了時刻、タイプ、所要時間）
   - セッションデータの保存（SQLite）

3. 統計・進捗表示
   - 今日の完了セッション数表示
   - 今日の合計集中時間表示

4. 通知機能
   - タイマー完了時のブラウザ通知（Notification API）
   - サウンド通知（Web Audio/Audio要素）

5. UI/UX
   - モックに合わせたカード型レイアウト
   - 円形プログレスバー
   - レスポンシブ対応（スマホ・PC両対応）
   - タップ操作最適化

6. バックエンドAPI
   - セッション記録用API（POST /api/sessions）
   - 統計取得用API（GET /api/stats?date=YYYY-MM-DD）
   - 入出力バリデーション（durationの負数チェックなど）

7. セキュリティ・拡張性
   - 最小構成では認証不要（将来拡張可能な設計）
   - DB移行対応（PostgreSQL等）

8. テスト
   - APIテスト（セッション登録・統計取得）
   - フロントエンドの状態遷移・表示更新テスト
